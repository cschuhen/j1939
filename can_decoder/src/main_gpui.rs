mod gpui;

fn main() {
    // Initialize GPUI runtime — Phase 1 Step 4 stub
    // Full implementation will create window with MainView (Phase 1 Steps 5-6)

    let _app = gpui::Application::new();
}
